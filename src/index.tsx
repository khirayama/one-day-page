import * as React from 'react';
import * as ReactDOM from 'react-dom';

window.addEventListener('DOMContentLoaded', () => {
  ReactDOM.render(<div>Hi</div>, window.document.querySelector('#root'));
});
